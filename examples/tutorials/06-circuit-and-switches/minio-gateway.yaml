apiVersion: argoproj.io/v1alpha1
kind: Gateway
metadata:
  name: minio-gateway
  labels:
    # gateway controller with instanceId "argo-events" will process this gateway
    gateways.argoproj.io/gateway-controller-instanceid: argo-events
spec:
  type: minio
  eventSourceRef:
    name: minio-event-source
  template:
    metadata:
      name: minio-gateway
      labels:
        gateway-name: minio-gateway
    spec:
      containers:
        - name: gateway-client
          image: metalgearsolid/gateway-client:v0.12
          imagePullPolicy: Always
        - name: minio-events
          image: metalgearsolid/minio-gateway:v0.12
          imagePullPolicy: Always
      serviceAccountName: argo-events-sa
  subscribers:
    http:
      - "http://circuit-sensor.argo-events.svc:9300/"
