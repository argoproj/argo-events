apiVersion: argoproj.io/v1alpha1
kind: EventSource
metadata:
  name: calendar-event-source
spec:
  type: "calendar"
  calendar:
    example:
      # creates an event every 10 seconds
      interval: "60s"
