apiVersion: argoproj.io/v1alpha1
kind: Sensor
metadata:
  name: kafka-example
  labels:
    sensors.argoproj.io/sensor-controller-instanceid: axis
spec:
  signals:
    - name: hello
  triggers:
    - name: "done-kafka-stream"
      message:
        body: "this is the message body"
        stream:
          type: kafka
          url: localhost:9092
          attributes:
            topic: gateway-controller
            partition: "1"
