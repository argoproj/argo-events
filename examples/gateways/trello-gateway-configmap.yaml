apiVersion: v1
kind: ConfigMap
metadata:
  name: trello-gateway-configmap
data:
  notification_1: |-
    hook:
     endpoint: "/test"
     port: "12000"
     url: "placeholderurl"
    apiKey:
      name: trello-secret
      key: apikey
    token:
      name trello-secret
      key: tokenkey
    description: "test webhook"
