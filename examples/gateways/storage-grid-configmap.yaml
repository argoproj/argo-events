apiVersion: v1
kind: ConfigMap
metadata:
  name: storage-grid-gateway-configmap
data:
  foo: |-
    port: "8080"
    endpoint: "/"
    events:
      - "ObjectCreated:Put"
    filter:
      suffix: ".txt"
      prefix: "hello-"
