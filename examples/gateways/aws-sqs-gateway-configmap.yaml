apiVersion: v1
kind: ConfigMap
metadata:
  name: aws-sqs-gateway-configmap
data:
  notification_1: |-
    accessKey:
      name: aws-secret
      key: access
    secretKey:
      name: aws-secret
      key: secret
    region: "us-east-1"
    queue: "<queue-name>"
    waitTimeSeconds: 50
