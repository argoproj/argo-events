apiVersion: v1
kind: ConfigMap
metadata:
  name: calendar-gateway-configmap
  namespace: argo-events
data:
  calendar.fooConfig: |-
    interval: 10s
  calendar.barConfig: |-
    schedule: "*/1 * * * *"
