apiVersion: argoproj.io/v1alpha1
kind: Gateway
metadata:
  name: mqtt-example-gateway
  namespace: argo-events
  labels:
    gateways.argoproj.io/gateway-controller-instanceid: axis
    gateway-name: "mqtt-example-gateway"
spec:
  image: "metalgearsolid/mqtt-gateway"
  imagePullPolicy: "Always"
  command: "/bin/mqtt-gateway"
  type: "mqtt"
  version: "1.0"
  serviceAccountName: "argo-events-sa"
  sensors: ["mqtt-example"]
