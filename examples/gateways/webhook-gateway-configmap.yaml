apiVersion: v1
kind: ConfigMap
metadata:
  name: webhook-gateway-configmap
data:
  webhook.portConfig: |-
    port: "12000"
    endpoint: "/bar"
    method: "POST"
  webhook.fooConfig: |-
    endpoint: "/foo"
    method: "POST"
