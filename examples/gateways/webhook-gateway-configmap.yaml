apiVersion: v1
kind: ConfigMap
metadata:
  name: webhook-gateway-configmap
  namespace: argo-events
data:
  # port can't be reconfigured.
  port: "12000"
  webhook.myConfig: |-
    endpoint: "/app"
    method: "POST"
  webhook.myOtherConfig: |-
    endpoint: "/other"
    method: "POST"
