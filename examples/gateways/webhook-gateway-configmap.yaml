apiVersion: v1
kind: ConfigMap
metadata:
  name: webhook-gateway-configmap
  namespace: argo-events
data:
  # port can't be reconfigured.
  webhoo.portConfig: |-
    port: "12000"
  webhook.fooConfig: |-
    endpoint: "/foo"
    method: "POST"
  webhook.barConfig: |-
    endpoint: "/bar"
    method: "POST"
