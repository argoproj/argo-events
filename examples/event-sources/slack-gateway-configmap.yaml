apiVersion: v1
kind: ConfigMap
metadata:
  name: slack-gateway-event-source
data:
  notification_1: |-
    hook:
     endpoint: "/event"
     port: "12000"
     url: "URL_OF_SERVICE"
    token:
      name slack-secret
      key: tokenkey
