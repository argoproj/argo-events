system_account: sys
authorization {
  token: "{{.Token}}"
}

accounts: {
  "sys": {
    "users": [
      {"user": "sys", "pass":"{{.SysPassword}}"}
    ]
  }
}

tls {
  cert_file: "server-cert.pem"
  key_file:  "server-key.pem"
  ca_file:   "rootCA.pem"
}