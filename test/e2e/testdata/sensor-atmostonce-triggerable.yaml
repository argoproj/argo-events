apiVersion: argoproj.io/v1alpha1
kind: Sensor
metadata:
  name: e2e-atmostonce-triggerable
spec:
  replicas: 1
  template:
    container:
      env:
        - name: DEBUG_LOG
          value: "true"
  dependencies:
    - name: test-dep-1
      eventSourceName: e2e-atleastonce
      eventName: example
  triggers:
    - template:
        name: log-trigger
        log: {}
