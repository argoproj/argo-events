apiVersion: v1
kind: Service
metadata:
  name: events-webhook
spec:
  ports:
    - port: 443
      # For Openshift compatibility (no root means no port < 1024).
      targetPort: 9443
  selector:
    app: events-webhook
